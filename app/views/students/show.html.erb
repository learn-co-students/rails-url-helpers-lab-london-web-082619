<h1><%= @student.to_s %></h1>

    <div><% if @student.active %></div>
        <p><%= "This student is currently active." %></p>
    <div><% else %></div>
        <p><%= "This student is currently inactive." %></p>
    <div><% end %></div>
    <%= link_to "activate", activate_student_path(@student.id) %>

      